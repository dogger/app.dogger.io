version: 2
updates:
- package-ecosystem: nuget
  directory: "/"
  schedule:
    interval: live
  open-pull-requests-limit: 1
  automerged_updates:
    - match:
        dependency_type: "all"
        update_type: "all"
